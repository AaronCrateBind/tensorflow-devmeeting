async function run() {
  console.log('Run!')
  // Step 0: Get Data
  // Step 1: Create Model
  // Step 2: Convert data to tensor
  // Step 3: Train Model
  // Step 4: Test Model
}

document.addEventListener('DOMContentLoaded', run);